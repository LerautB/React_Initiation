const Formation = ({ title, descr }) => {
	return (
		<div class='formation-item'>
			<h3 class='formation-item-title'>{title}</h3>
			<p class='formation-item-descr'>{descr}</p>
		</div>
	)
}

export default Formation
