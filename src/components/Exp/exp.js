const Exp = ({title,descr}) => {
    return (
        <div class="exp-item">
            <h3 class="exp-item-title">{title}</h3>
            <p class="exp-item-descr">{descr}</p>
        </div>
    )
}

export default Exp